import React from 'react'

// context 16.x  以上版本

// React.createContext() => obj  对象里面有两个东西 Provider/Consumer

let {Provider, Consumer} = React.createContext();

export {
    Provider,
    Consumer
}